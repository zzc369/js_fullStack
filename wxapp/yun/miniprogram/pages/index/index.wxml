<block wx:for="{{products}}">
  <van-card
    price="{{item.view}}"
    desc ="{{item.color}}"
    title ="{{item.title}}"
    thumb = "{{item.image}}"
    data-id="{{item._id}}"
    binftap = "click"
    />
</block>